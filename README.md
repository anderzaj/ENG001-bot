# ENG001-bot
Un bot que haga el curso online de ingles de la UAI por mi

necesita un archivo "secrets.js" que exporta un objeto de la forma

```
const keys = {
  username: <tu usuario>,
  password: <tu clave>
}

module.exports = keys;
```

para ser usado en index.js para acceder a la plataforma
